# Hunter (artifact / review)

This bundled skill is fully implemented in the Claude Code TypeScript CLI when `REVIEW_ARTIFACT` is enabled. In the Go runtime, use the TypeScript CLI or enable the feature in a TS-based session for the complete workflow.
