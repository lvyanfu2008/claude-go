package toolexecution

// SanitizeToolNameForAnalytics is a stub for src/services/analytics/metadata.ts sanitizeToolNameForAnalytics.
// TODO(toolExecution.ts): match TS sanitization rules when telemetry is wired.
func SanitizeToolNameForAnalytics(name string) string {
	return name
}
